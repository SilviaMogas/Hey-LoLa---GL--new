# Hey Lola — agent notes

## Brand copy (temporary restrictions — May 2026)

When writing user-facing copy, SEO meta, OG/Twitter tags, JSON-LD descriptions
or any marketing string, **do not** mention:

- "pet records", "digital pet passports", "records" as a feature
- "travel", "travel concierge", "real-time maps", "maps" as a feature

The OG image PNG at `public/og-image.png` still has the old strapline baked
in ("BOUTIQUE PET LIFESTYLE — TRAVEL CONCIERGE" / "Real-time maps. Digital
pet passports.") — it needs a re-render before the next launch, but do not
echo that copy anywhere else in the meantime.

### Canonical title

- HTML `<title>` and `og:title` / `twitter:title`:
  `Hey Lola — Your dog's lifestyle concierge`
- Tone reference: editorial, unhurried — see the waitlist repo
  `SilviaMogas/HeyLola_WaitingList_23May` (`app/layout.tsx`).
