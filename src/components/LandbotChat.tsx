/**
 * LandbotChat — DISABLED.
 *
 * The third-party Landbot Livechat widget (the floating bubble in the bottom
 * corner) has been removed from Hey Lola at the owner's request. This file is
 * intentionally a no-op so that even if something still imports it, no external
 * script is ever injected and no chat bubble ever appears.
 *
 * Do not re-add the Landbot loader here without an explicit decision.
 */
export const LandbotChat: React.FC = () => null;
